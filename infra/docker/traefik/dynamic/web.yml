http:
  routers:
    web:
      rule: "PathPrefix(`/`)"
      entryPoints:
        - web
      service: web

  services:
    web:
      loadBalancer:
        servers:
          - url: "http://web:80"
