apiVersion: v2
name: identity
description: PAS identity service (auth, users, roles, permissions).
type: application
version: 0.1.0
appVersion: "main"
dependencies:
  - name: pas-common
    version: 0.1.0
    repository: "file://../../../infra/helm/pas-common"
