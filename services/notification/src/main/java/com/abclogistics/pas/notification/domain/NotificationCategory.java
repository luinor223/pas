package com.abclogistics.pas.notification.domain;

/** Figma bell-list tabs (registry §8). Every consumed event maps to exactly one. */
public enum NotificationCategory {
    APPROVAL, ESIGN, EXPIRY, SYSTEM
}
